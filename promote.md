你是一位专业的前端开发专家。请创建一个完整的单文件HTML网页，所有代码（HTML、CSS、JavaScript）必须内嵌在同一个文件中，使用<style>标签放置CSS，使用<script>标签放置JavaScript。不要使用任何外部链接、CDN或框架（如Bootstrap、jQuery），纯原生实现。

网页主题：[在线录屏工具, 一个简单的在线录屏工具，用户可以在浏览器中录制屏幕并下载视频。]

具体要求：

- 响应式设计，支持移动端和桌面端。
- 现代简约风格，颜色方案：[指定颜色，例如：主色蓝调#007BFF，背景白色，深色模式可选]。
- 页面结构包括：[列出部分，例如：导航栏、英雄区（Hero Section）、关于区、服务区、联系表单、页脚]。
- 交互效果：[例如：平滑滚动、按钮悬停动画、暗黑模式切换、简单表单验证]。
- 添加代码注释，解释关键部分。
- 确保代码干净、可读、兼容现代浏览器。
- 标题为：[网页标题]，meta标签包括描述和关键词。

创建文件夹，命名为[网页标题]，在文件夹中创建一个HTML文件，文件名也为[index].html。
